export function buildBrandClusters(brand: string) {
  return [
    `${brand} engines reliability`,
    `${brand} diesel engines`,
    `${brand} common problems`,
    `${brand} maintenance cost`,
    `${brand} most reliable models`
  ]
}